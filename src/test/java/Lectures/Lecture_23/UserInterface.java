package Lectures.Lecture_23;

public interface UserInterface {
    static void getUserName() {
    }
    // дефолтные методы интерфейса не обязательно реализовывать в классе, который имплиментится от интерфейса.
    // статические методы интерфейса не обязательно реализовывать в классе, который имплиментится от интерфейса.
    // остальные методы интерфейса (не дефолтные и не статичные) обязательно необходимо реализовывать в классе, который имплиментится от интерфейса.
}
